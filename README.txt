V7 CERTIFICATE GALLERY + INTERACTIVE CONTACT

Built directly on V6 Verified Credentials.

CERTIFICATIONS:
- Small visual certificate cards using previews of the exact supplied certificates.
- Horizontal gallery, drag/scroll enabled.
- Auto-cycles one certificate at a time every 2.6 seconds.
- Hover pauses cycling.
- Animated active-card focus, dots, image scan and zoom.
- Clicking opens the original certificate PDF/JPEG in a new tab.

CONTACT:
- Rebuilt as an interactive analytics contact console.
- Email, LinkedIn and GitHub links.
- Animated Bhopal/India radar.
- Animated channel-ready state and hover motion.
- Working message form: validates fields and opens the visitor's email client with recipient, subject and message prefilled.
- No backend is required for the mail-client handoff.

All V6 project, hero, skills, Business Signal Monitor, theme and analytics animations remain.

V7.1 BUG FIX:
- Fixed automatic page jumping to Certifications every 2.6 seconds.
- Root cause: certificate autoplay used Element.scrollIntoView(), which also scrolled the main webpage.
- Carousel now scrolls only the .cert-gallery container with horizontal scrollTo().
- Automatic certificate cycling and card animations remain active.

V7.2 CERTIFICATE SLIDER UPDATE:
- Certificate gallery now advances automatically every exactly 3 seconds.
- Sequence: 01 → 02 → 03 → 04 → 05 → 06 → 01.
- Only the horizontal certificate gallery moves.
- The main webpage never jumps to the certificate section.
- Hover pauses autoplay so a visitor can inspect a certificate.
- Mouse/touch manual scrolling remains available.
